{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <form action="/locations" method="POST">
        <fieldset>
            <legend>Create New Location</legend>
            <label class="form-label">Title: </label>
            <input 
                type="text" 
                name="title" 
                placeholder="{{ location.title }}"
                value="{{ location.title }}"
                class="form-control"
            >
            <label class="form-label">Body:</label>
            <input 
                type="text" 
                name="body" 
                placeholder="{{ location.body }}" 
                value="{{ location.body }}" 
                class="form-control"
            >
            <label class="form-label">Amount:</label>
            <input 
                type="number" 
                name="amount"
                value="{{ location.amount }}"
                class="form-control"
            >
            <label class="form-label">Ready:</label>
            <input 
                type="checkbox" 
                name="ready" 
                class="form-check-input"
                {% if location.ready %}
                    checked
                {% endif %}
            >
        </fieldset>
        <input type="submit" value="Edit Location" class="btn btn-success">
    </form>
</div>
{% endblock %}