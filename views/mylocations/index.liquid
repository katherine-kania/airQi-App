{% layout 'layout.liquid' %}
{% block content %}

  <div class="container-sm con-pad">
    <h2>All my saved locations</h2>
    {{ location.search}}
    <div>
      {% for location in locations %}
      <h3>
        {{ location.name }}
      </h3>

      <a href="/location/{{ location.search }}" class="btn btn-info">current conditions</a>
      {% if location.owner == userId  %}
      <form action="/mylocations/{{ location.id }}?_method=DELETE" method="POST">
        <input type="submit" value="DELETE" class="btn btn-danger">
      </form>
      {% endif %}
      {% endfor %}
    </div>
  </div>

{% endblock %}