{% layout 'layout.liquid' %}
{% block content %}
<div class="container-sm">
    <form action="/places" method="POST">
        <fieldset>
            <legend>Create New Place</legend>
            <label class="form-label">Title: </label>
            <input 
                type="text" 
                name="title" 
                placeholder="{{ place.title }}"
                value="{{ place.title }}"
                class="form-control"
            >
            <label class="form-label">Body:</label>
            <input 
                type="text" 
                name="body" 
                placeholder="{{ place.body }}" 
                value="{{ place.body }}" 
                class="form-control"
            >
            <label class="form-label">Amount:</label>
            <input 
                type="number" 
                name="amount"
                value="{{ place.amount }}"
                class="form-control"
            >
            <label class="form-label">Ready:</label>
            <input 
                type="checkbox" 
                name="ready" 
                class="form-check-input"
                {% if place.ready %}
                    checked
                {% endif %}
            >
        </fieldset>
        <input type="submit" value="Edit Place" class="btn btn-success">
    </form>
</div>
{% endblock %}